import { useState } from "react";
import VenteFlashImage from "./VenteFlashImage";
import VenteFlashMenu from "./VenteFlashMenu";

const VenteFlash = () => {
    const [categoryId, setCategoryId] = useState<number | null>(null);

    return (
        <>
        </>
    );
};